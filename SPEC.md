# ivvvy 仕様書

## 概要

ivvvy はテキストに特化したドキュメント管理ツールである。
Markdown形式のフラットファイルをブラウザ上で閲覧・編集・検索でき、
シングルバイナリとして配布可能なGoアプリケーションとして実装されている。

## アーキテクチャ

### 技術スタック

| 領域 | 採用技術 | バージョン |
|------|----------|------------|
| 言語 | Go | 1.26+ |
| Webサーバー | net/http + go-chi/chi | v5 |
| Markdown変換 | yuin/goldmark | - |
| フロントマター | go.abhg.dev/goldmark/frontmatter | - |
| 形態素解析 | ikawaha/kagome (IPA辞書) | v2 |
| 静的アセット埋め込み | Go embed | 標準 |

### ディレクトリ構成

```
ivvvy/
├── main.go                    # エントリーポイント
├── internal/
│   ├── page/
│   │   ├── page.go            # Page構造体・FrontMatter構造体
│   │   ├── store.go           # ファイルI/O・CRUD操作・オンメモリキャッシュ・タグ集計
│   │   ├── history.go         # ページ版管理
│   │   └── store_test.go      # ストアのユニットテスト
│   ├── markdown/
│   │   ├── renderer.go        # goldmarkによるMarkdown→HTML変換
│   │   ├── wikilink.go        # [[WikiLink]] カスタムパーサー（パイプ記法対応）
│   │   ├── callout.go         # Obsidian/Quartz形式Callout変換
│   │   └── renderer_test.go   # レンダラのユニットテスト
│   ├── generate/
│   │   └── generate.go        # 静的サイトジェネレータ
│   ├── search/
│   │   ├── indexer.go         # kagome形態素解析 + JSON生成
│   │   └── indexer_test.go    # インデクサーのユニットテスト
│   └── server/
│       ├── server.go          # HTTPサーバー・ルーティング定義
│       ├── handler.go         # 各エンドポイントのハンドラ
│       └── handler_test.go    # ハンドラのユニットテスト
├── web/
│   ├── static/
│   │   ├── css/style.css      # スタイルシート（印刷用 @media print 含む）
│   │   ├── js/search.js       # クライアントサイド検索（スニペット付き）
│   │   ├── js/editor.js       # タブ切替エディタ + タグ入力サジェスト
│   │   ├── js/settings.js     # テキストサイズ・カラーテーマ設定管理
│   │   ├── js/keyboard.js     # キーボードショートカット + ヘッダのON/OFFトグル
│   │   ├── js/graph.js        # グラフビュー初期化（cytoscape.js 連携）
│   │   ├── js/cytoscape.min.js # cytoscape.js v3 ライブラリ（MIT、同梱）
│   │   └── embed.go           # //go:embed ディレクティブ
│   └── template/
│       ├── layout.html        # 共通レイアウト
│       ├── index.html         # トップページ
│       ├── view.html          # ページ閲覧
│       ├── edit.html          # ページ編集（タブ切替方式）
│       ├── list.html          # 全ページ一覧
│       ├── tags.html          # タグ一覧
│       ├── tag_pages.html     # タグ別ページ一覧
│       ├── graph.html         # グラフビュー画面
│       ├── page_history.html  # ページの過去バージョン一覧
│       └── embed.go           # //go:embed ディレクティブ
└── data/                      # Markdownファイル保存先
    └── history/               # 過去バージョン保管
        └── {pageID}/
            └── {UnixMilli}.md
```

## 起動方法

```bash
# デフォルト設定で起動（ポート3000、実行ファイルと同じ場所の data ディレクトリ）
./ivvvy

# カスタム設定
./ivvvy -port 8080 -data /path/to/documents

# IP制限つき
./ivvvy -allow-ip "127.0.0.1/32,192.168.1.0/24"

# Basic認証つき
./ivvvy -basic-auth "alice:secret"

# IP制限とBasic認証を併用
./ivvvy -allow-ip "192.168.0.0/16" -basic-auth "user:pass"

# 静的サイトを生成（サーバーは起動しない）
./ivvvy -generate ./out -data /path/to/documents
```

### コマンドライン引数

| 引数 | デフォルト | 説明 |
|------|------------|------|
| `-port` | 3000 | HTTPサーバーのポート番号 |
| `-data` | 実行ファイルと同じ場所の `data` | Markdownファイルの保存先ディレクトリ |
| `-allow-ip` | （空） | アクセス許可IPのCIDR（カンマ区切り）。空なら制限なし |
| `-basic-auth` | （空） | Basic認証 `"user:pass"` 形式。空なら認証なし |
| `-generate` | （空） | 静的HTMLサイトの出力先ディレクトリ。指定するとサーバーは起動しない |

`-data` が未指定の場合、実行ファイルのパスを `os.Executable()` で取得し、その親ディレクトリ配下の `data` を使用する。パス取得に失敗した場合のみ `./data`（カレントディレクトリの `data`）にフォールバックする。

## ビルド

```bash
go build -o ivvvy .
```

シングルバイナリ（約27MB）として生成される。
HTML/CSS/JS/テンプレートはすべて `//go:embed` でバイナリに埋め込まれる。

## テスト

```bash
go test ./internal/... -v
```

### テストカバレッジ

- **page/store**: CRUD操作、ソート順、レガシーフロントマター互換性、BuildGraph（孤立ノード・壊れたリンク・自己ループの扱い）
- **markdown/renderer**（9テスト）: 基本変換、WikiLink（英字・日本語・パイプ記法）、コードブロック、GFMテーブル、Callout（note・カスタムタイトル・通常blockquoteとの区別）
- **search/indexer**（3テスト）: 辞書初期化、インデックス生成、形態素解析精度（助詞除外）
- **server/handler**: グラフビュー画面のレンダリング、グラフAPI のJSONレスポンス

## データモデル

### Page構造体

```go
type Page struct {
    ID        string    // ミリ秒タイムスタンプ自動採番（ファイル名）
    Title     string    // フロントマター title
    Body      string    // Markdown本文
    Tags      []string  // フロントマター tags
    CreatedAt time.Time // ファイル作成日時
    UpdatedAt time.Time // ファイル更新日時
}
```

### ファイル形式

すべてのページはYAMLフロントマター付きのMarkdownファイルとして保存される。
ファイル名はミリ秒タイムスタンプ（例: `1715644800000.md`）。

```markdown
---
title: "ページタイトル"
tags:
  - "タグ1"
  - "タグ2"
---

# 見出し

本文のMarkdown...
```

タグが空の場合、`tags:` フィールドはフロントマターに出力されない。

**既存データ互換性**: 旧バージョンの ivvvy が書き出した `path:` 等の
未知のフロントマターキーは、goldmark/frontmatter のデフォルト挙動で
読み込み時に自動的に無視される。ファイルを書き換える必要はない。

## オンメモリキャッシュ

- 全ページをメモリ上の `map[string]*Page` にキャッシュし、読み取り時のディスクI/Oを排除
- 起動時に `loadAllToCache()` でデータディレクトリの全 `.md` ファイルを読み込む
- `Get()`, `List()`, `BuildGraph()` はすべてキャッシュから返す
- `Save()` はディスク書き込みとキャッシュ更新を同期的に実行
- `Delete()` はディスク削除とキャッシュ削除を同期的に実行
- 並行アクセスは `sync.RWMutex` で保護（読み取りは共有ロック、書き込みは排他ロック）
- キャッシュ汚染を防ぐため、外部に返すPageは `copyPage()` でコピーを返す（Tags スライスも含む）
- パースに失敗したファイルはスキップされ、壊れたファイルがあっても起動可能

## APIエンドポイント

### 画面系（HTML）

| メソッド | パス | 説明 |
|----------|------|------|
| GET | `/` | トップページ（検索窓のみのシンプルなレイアウト） |
| GET | `/pages` | 全ページ一覧 |
| GET | `/page/{id}` | ページ閲覧（存在しない場合は新規作成へリダイレクト） |
| GET | `/page/{id}/edit` | ページ編集画面（タブ切替方式） |
| GET | `/page/{id}/history` | ページの過去バージョン一覧 |
| GET | `/page/{id}/history/{filename}` | 指定した過去バージョンの表示 |
| GET | `/new` | 新規ページ作成画面（ID自動採番） |
| GET | `/tags` | タグ一覧 |
| GET | `/tags/{tag}` | タグ別ページ一覧 |
| GET | `/graph` | グラフビュー（ページ間 WikiLink を可視化） |
| GET | `/static/*` | CSS/JS静的ファイル |

### API系（JSON）

| メソッド | パス | 説明 |
|----------|------|------|
| POST | `/api/page` | ページ新規作成（ID省略時は自動採番） |
| PUT | `/api/page/{id}` | ページ更新 |
| DELETE | `/api/page/{id}` | ページ削除 |
| GET | `/api/page/{id}/raw` | Markdownソース取得（tags含む） |
| POST | `/api/preview` | Markdown→HTMLプレビュー変換 |
| GET | `/api/graph.json` | グラフデータ（nodes/edges のJSON） |
| GET | `/api/tags` | 全タグ名の一覧（JSON配列） |
| GET | `/search/index.json` | 検索インデックスJSON（本文含む） |

### APIリクエスト形式

```json
{
  "id": "（省略可能。省略時はミリ秒タイムスタンプで自動採番）",
  "title": "ページタイトル",
  "body": "Markdown本文",
  "tags": ["タグ1", "タグ2"]
}
```

※ ページ保存後、全ページのWikiLinkキーワード自動書き換えが実行される。

## ページ間の関連付け

### WikiLink

- `[[ページID]]` 記法でページ間リンクを記述
- `[[ページID|表示テキスト]]` パイプ記法でリンクテキストを指定可能
- HTMLレンダリング時に `<a href="/page/ページID" class="wikilink">` に変換される
- パイプ記法の場合、hrefにはパイプ前のID、リンクテキストにはパイプ後の表示テキストを使用
- goldmarkのカスタムインラインパーサー（優先度50）として実装
- ページ保存時、本文中の `[[キーワード]]` がページタイトルと一致する場合、自動的に `[[ID|キーワード]]` に書き換えられる（キーワードWikiLinkフロー）

#### キーワードWikiLinkの設計

ivvvy のページIDは「ファイル名にもなるミリ秒タイムスタンプ等の数値」であり、
ユーザーが意識する「キーワード」（=自然な日本語のページ名）とは独立している。
両者を分離した上で、編集体験としては「キーワードでリンクを書く」ことを許す。
そのつなぎ込みを次の4ステップのフローで実現する：

1. **キーワード記法で書く**: ユーザーは本文に `[[Go言語]]` のように人間が読める
   キーワードで WikiLink を記述する。この時点ではまだIDとの対応は付いていない。
2. **未解決リンクのクリックで新規作成へ誘導**: そのリンクは `/page/Go言語` への
   HTTP GET を引き起こすが、該当IDのページは存在しない。ハンドラはこれを検知して
   新規作成画面へリダイレクトし、キーワードをタイトルの初期値として渡す。
3. **ページ作成によりキーワードとIDが結びつく**: ユーザーが新規ページを保存すると、
   新しいIDが採番され、タイトルにキーワードが入った状態のページがストアに登録される。
   この時点で「キーワード ↔ ID」のマッピングが初めて成立する。
4. **保存時の逆引きリライト**: **これが本フローの肝**。新しくページを保存した
   タイミングで、**ストア内の全ページの本文を走査**し、本文中に出現する
   `[[キーワード]]`（パイプ無し記法）のうち、保存されたページのタイトルと一致するものを
   `[[ID|キーワード]]` に書き換える。これにより、過去に未解決状態で書かれていた
   キーワードリンクが、新ページ作成と同時に正しいIDへ自動接続される。

##### 全件走査を許容する設計判断

ステップ4は **全ページ本文の線形探索** を伴う。インデックス等の最適化を入れず
あえて全件走査としているのは、次の性質を守りたいからである：

- **キーワード（人間が読む名前）とID（システムが扱う識別子）を完全に切り離す**
  ことが、本ツールの根幹方針である。
  - ファイル名にキーワード（例: `Go言語.md`）を使えばこの問題は起きないが、
    その場合「タイトル変更＝ファイル名変更＝既存リンク全破壊」となり、
    あるいは「同名タイトルが作れない」「ファイル名に使えない文字が問題化する」
    といった、ファイルシステム由来の制約に縛られてしまう。
  - 数値IDで一意性と不変性を担保し、表示用のキーワードを完全に自由化する設計は、
    obsidian など多くのMarkdownベースツールが採用するファイル名兼ねIDの方式とは
    意図的に異なる選択である。
- 個人利用規模（数百〜数千ページ）であれば、ページ保存という低頻度操作で
  全件走査してもコストは無視できる。性能のために本質的な疎結合性を犠牲にしない。
- インデックスを持たないため、本文の編集・タイトル変更・ID採番のいずれが
  起きてもキャッシュ整合性の心配が無く、実装も明快に保てる。

この方針は WikiLink パーサーの仕様（パイプ無しはキーワード、パイプ有りはID）と
ペアになっており、ストア層・テンプレート層に「キーワード」を一切持たせない
ことで全体の責務分離を実現している。

### Callout（コールアウト）

- Obsidian/Quartz形式の `> [!type]` 記法に対応
- blockquote内の最初の行が `[!type]` パターンに一致する場合、Calloutブロックに変換される
- `> [!type] カスタムタイトル` でタイトルをカスタマイズ可能（省略時はtypeのデフォルト名）
- 通常のblockquote（`[!type]` パターンなし）はそのままblockquoteとして出力される
- goldmarkのASTTransformerとして実装

#### 対応するCalloutタイプ

| タイプ | デフォルトタイトル | アイコン | 背景色 |
|--------|-------------------|---------|--------|
| note | Note | ペンシル | 青系 |
| tip | Tip | 炎 | 緑系 |
| warning | Warning | 三角 | 黄色系 |
| danger | Danger | 八角形 | 赤系 |
| info | Info | サークルi | 青系 |
| example | Example | リスト | 紫系 |
| quote | Quote | 引用符 | グレー系 |
| abstract | Abstract | クリップボード | 水色系 |
| todo | Todo | チェックサークル | 緑系 |
| bug | Bug | バグ | 赤系 |

- アイコンはLucide風のインラインSVGで描画（`stroke="currentColor"` でCSS色を継承）

## タグ機能

ページにタグを付与し、分類・絞り込みを行う機能。

### データモデル

- `Page.Tags []string` としてページ構造体に保持
- フロントマターの `tags:` フィールド（YAML配列）として永続化
- タグが空の場合、フロントマターには `tags:` を出力しない

### タグ入力UI

- 編集画面にタグ入力欄を配置（ピル表示 + テキスト入力）
- Enter キーまたはカンマで新しいタグを追加、Backspace で末尾のタグを削除
- `/api/tags` から既存タグ一覧を取得し、入力中にサジェスト候補を表示
- サジェストは選択済みタグを除外し、入力テキストで部分一致フィルタリング

### 画面

- **タグ一覧** (`/tags`): 全タグをアルファベット順に表示。各タグにページ数を併記
- **タグ別ページ一覧** (`/tags/{tag}`): 指定タグを持つページを更新日時降順で一覧表示

### ストア層

- `TagCounts()`: 全ページのタグを集計し `map[string]int` を返す
- `ListAllTags()`: 全タグをアルファベット順で返す
- `ListByTag(tag)`: 指定タグを持つページを更新日時降順で返す

### API

- `GET /api/tags`: 全タグ名をJSON配列で返す（エディタのサジェスト用）

## 検索機能

### アーキテクチャ

1. **サーバーサイド**: kagome（IPA辞書）で全ページの本文を形態素解析し、トークン化済みJSONインデックスを生成。本文原文もインデックスに含める。
2. **クライアントサイド**: ブラウザが `/search/index.json` を取得し、JavaScriptでAND方式の部分一致検索を実行。検索結果にはキーワード周辺のスニペット（前後40文字）をハイライト付きで表示。

### トークン化ルール

- 助詞・助動詞・記号は除外（検索ノイズの低減）
- 名詞・動詞・形容詞等がスペース区切りで結合される
- タイトルと本文を結合してトークン化

## グラフビュー

ページ間の WikiLink を有向グラフとして可視化する画面（`/graph`）。
プロジェクト名 ivvvy = ivy（ツタ）の由来にちなんで配色は緑基調で統一する。

### 描画ライブラリ

- [cytoscape.js](https://js.cytoscape.org/) v3 系の minified 版を `web/static/js/cytoscape.min.js` として同梱
- ライセンス MIT、約 365KB。CDN 依存を避けるためバイナリ同梱とする
- レイアウトアルゴリズムは `cose`（Compound Spring Embedder、cytoscape 同梱）
  - 追加プラグイン不要で孤立ノードも周辺に自然配置される
- グラフの初期化・スタイル適用・イベント処理は `web/static/js/graph.js` に集約

### データAPI（`/api/graph.json`）

```json
{
  "nodes": [
    {"id": "1715644800000", "title": "ページタイトル"}
  ],
  "edges": [
    {"source": "1715644800000", "target": "1715644900000"}
  ]
}
```

構築は `Store.BuildGraph()` で行う。仕様:
- 全ページがノードに含まれる（リンクなしの**孤立ノードも含む**）
- 各ページの本文から `extractWikiLinkTargets()` で WikiLink を抽出してエッジに変換
- **存在しないターゲットへのリンク**（壊れたリンク）はエッジに含めない
- **自己ループ**（source==target）はエッジに含めない
- 同一の (source, target) 組は重複排除して 1 本だけ含める
- ノードは ID 昇順、エッジは (source, target) の辞書順にソートして返す（決定的出力）

### カラーパレット（ivy 緑基調）

| 用途 | ライトモード | ダークモード |
|------|-------------|-------------|
| ノード塗り | `#4a8a5a`（深緑） | `#6dbb83`（明るい緑） |
| ノード境界 | `#2d5a3d` | `#9dd4a8` |
| ノードラベル | `#2d3a2d` | `#d8e8d8` |
| エッジ | `#7ab088` | `#4a7a58` |
| ホバー/選択アクセント | `#6dbb83` | `#a8e6b8` |

ダークモード時もブルー系アクセント（`#6ea8ff`）には切り替えず、緑基調で統一する。

### インタラクション

- **ノードクリック**: 該当ページ（`/page/{id}`）へ遷移
- **ドラッグ**: ノードの位置を手動調整
- **マウスホイール**: ズーム（`minZoom: 0.2`, `maxZoom: 3`）
- **テーマ切替**: `MutationObserver` で `html[data-theme]` 属性の変化を監視し、スタイルを再適用

## バックリンク一覧

ページ閲覧画面の最下部に、当該ページへの WikiLink を含むページの一覧を「このページへのリンク」として表示する。

### 実装方式

- **逆引きインデックス**: `Store` 構造体に `backlinks map[string][]string`（キー: リンク先ページID、値: リンク元ページIDのスライス）を保持する
- **起動時構築**: `buildBacklinksIndex()` で全ページの本文から `[[ID]]` / `[[ID|表示テキスト]]` パターンを正規表現で抽出し、逆引きマップを構築する
- **差分更新**: `Save()` / `Delete()` 時に `rebuildBacklinksForPage()` で該当ページ分のみ更新する（旧エントリ削除→新エントリ追加）
- **O(1) 参照**: ページ閲覧時は `GetBacklinks(targetID)` でマップ参照するのみ。閲覧ごとの全文検索は行わない
- 結果はタイトルの昇順でソートされる
- WikiLink ターゲットIDの抽出には `extractWikiLinkTargets()` 関数を使用（パッケージレベルの正規表現 `wikiLinkPattern` をコンパイル済みで保持）

## ページ単位の版管理

各ページの保存時に「上書き直前の内容」をタイムスタンプ付きで `history/{ID}/{UnixMilli}.md` として保存する。

#### ディレクトリ構造

```
{dataDir}/
├── {pageID}.md                  # 現行（最新）バージョン
└── history/
    └── {pageID}/
        ├── 1734567890123.md     # 過去バージョン（タイムスタンプ降順表示）
        ├── 1734567990456.md
        └── ...
```

#### 実装方式

- 新規型 `internal/page/history.go` の `HistoryStore` が版管理を担当する。`Snapshot` / `ListVersions` / `LoadVersion` の3メソッドを提供
- `Store.Save` 内で上書き直前に既存ファイルを読み出し、`HistoryStore.Snapshot` を呼んで退避する
- 新規作成（既存ファイルなし）時は退避をスキップ＝初回保存では履歴ゼロ件
- ファイル名は `time.Now().UnixMilli()` ベース。同一ミリ秒で衝突した場合は 1ms ずつインクリメントして空きを探す
- フロントマターのパースは `Store.loadPageFromDisk` から共通ヘルパー `parsePageBytes` に切り出し、`HistoryStore.LoadVersion` でも再利用する
- セキュリティ: `LoadVersion` の `filename` 引数は「数字.md」形式のみ許可し、ディレクトリトラバーサルを防ぐ

#### ルーティング・テンプレート

| URL | ハンドラ | テンプレート | 用途 |
|-----|----------|------------|------|
| `GET /page/{id}/history` | `handlePageHistory` | `page_history.html` | 過去バージョン一覧（新しい順） |
| `GET /page/{id}/history/{filename}` | `handlePageVersionView` | `view.html` 流用 | 指定バージョンの内容表示 |

- 一覧画面の各行は変更日時のテキストリンク（押すと該当版に遷移）
- 過去版表示時は `view.html` を流用しつつ `IsVersionView=true` でバナーを表示、編集/履歴リンクを非表示にする
- ページ閲覧画面（`view.html`）の `ID:xxx Title:xxx Update:YYYY/MM/DD` 行に、編集ボタンの左にテキストリンク「履歴」を追加した
- 静的サイト生成モード（`IsStatic`）では履歴ページを生成せず、リンクも表示しない

#### ページ削除との関係

ページ本体（`{dataDir}/{ID}.md`）を `DELETE /api/page/{id}` で削除しても、履歴ディレクトリ `history/{ID}/` は意図的に残す。誤削除からの復旧用途を兼ねる。

## UIデザイン方針

- テキストのみの利点を生かした余白の多いデザイン
- サイドメニューなし
- トップのナビゲーション（トップ・ページ・タグ・グラフ・新規作成 + テキストサイズ切替 + テーマ切替 + キーボードショートカットON/OFFトグル）
- トップページはGoogleスタイルの検索窓のみのシンプルなレイアウト
- レスポンシブ対応（768px以下でモバイルレイアウト）
- 編集画面は「編集」「プレビュー」のタブ切り替え方式（全幅表示）
- プレビューはサーバーサイドAPIで変換（GFMテーブル・全レベル見出し等に正確対応）
- コードブロック・インラインコードには等幅フォントを明示的に適用
- 検索結果はタイトル＋スニペットのみ表示
- 全ページ一覧にはIDをタイトル前に表示
- タグ一覧はチップ形式でページ数を併記
- Calloutブロックはタイプごとに異なる背景色とLucide風SVGアイコンで表示
- 存在しないページへのWikiLinkアクセス時、キーワードをタイトルとした新規作成画面へリダイレクト
- 行間と余白を広めに取り（body `line-height: 1.9`、本文 `line-height: 2.0`、本文幅 `760px`）、長文の可読性を優先
- ページ閲覧画面のヘッダはコンテンツの一部としての h1 を持たず、`ID:xxx Title:xxx Update:YYYY/MM/DD` 形式のラベル付きメタ行 + 履歴リンク + 編集ボタンで構成する。Web表示と印刷でレイアウト差が出ないことを優先した結果
- メタ行のテキストはすべて同じ小サイズ（0.8rem）で並列表示し、視覚的に控えめにする
- `.page-header` には下線や背景色を付けず、本文と一体化させる
- サイト全体のヘッダ（ナビゲーション）の背景色指定は `body > header` で限定し、article 内の `<header class="page-header">` に波及させない
- ページ閲覧画面のタグはメタ行の下にチップ形式で表示

## キーボードショートカット

ヘッダ右端のキーボードアイコンクリックで ON/OFF を切り替える。状態は `localStorage.ivvvy.shortcuts` に保存され、再訪時も継続する。OFF時はアイコンが薄く表示される。

アイコンへのマウスオーバーでショートカット一覧のツールチップが即時表示される。ツールチップ内には現在のON/OFF状態と、編集画面では無効になる旨の注意書きも表示する。

### ナビゲーション系（Gmail風の連続キー）

`g` を押した後 1.5 秒以内に次のキーを押すと遷移する。

| キー | 遷移先 |
|------|--------|
| `g` → `p` | ページ一覧 |
| `g` → `r` | グラフビュー |
| `g` → `n` | 新規作成 |
| `g` → `i` | トップ（移動後に検索窓にフォーカス） |
| `g` → `g` | 現在ページの先頭にスクロール（Vim風） |

### ページ操作系

| キー | 動作 |
|------|------|
| `e` | 閲覧中のページを編集（編集ボタンが存在する場合のみ） |
| `j` | 下方向にスクロール（ビューポート高の約50%） |
| `k` | 上方向にスクロール（ビューポート高の約50%） |
| `G` | 現在ページの末尾にスクロール（Vim風） |

### 編集画面での挙動

編集／新規作成画面（`<form id="page-form">` を持つページ）では、**すべてのショートカットを完全に無効化する**。

- 編集中にナビ系のキー（`g p` 等）で誤って画面遷移してしまうのを防ぐため
- 保存もボタンを明示的に押す運用とし、`Cmd/Ctrl + S` などの保存系ショートカットは設けない
- `<input>` / `<textarea>` / `contenteditable` フォーカス中の入力は通常入力として通す
- 実装は `web/static/js/keyboard.js` の単一ファイル、`layout.html` の末尾で読み込む

## テキストサイズ切替

ナビゲーションバーのテーマ切替アイコンの左隣にテキストサイズアイコン（T字のSVG）を配置する。クリックでポップアップが開き、「大」「標準」「小」の3段階から選択できる。

- `html` 要素の `data-textsize` 属性で制御する
- `large`: `font-size: 19px`、`small`: `font-size: 13px`、`normal`（デフォルト）: `16px`
- 既存CSSが `rem` ベースのため、ルートの `font-size` 変更で全体に波及する
- 設定は `localStorage` キー `ivvvy.textsize` に保存され、再訪時も維持される
- FOUC（Flash of Unstyled Content）防止のため、`<head>` 内のインラインスクリプトで即座に `data-textsize` 属性を復元する

## カラーテーマ

デフォルトのライトモードと暗転のダークモードの2モードのみに絞った。

- `html[data-theme="dark"]` のみが有効値。属性なしがライトモード（デフォルト）
- ダークモードは中立的なグレースケール基調（背景 `#1a1a1a`、テキスト `#e5e5e5`、控えめなブルーアクセント `#6ea8ff`）
- 旧テーマ名（`nordic` / `cyber` / `luxury` / `chiffon`）が `localStorage` に残っている場合はライト扱いに正規化する後方互換処理を `settings.js` に入れた
- UIはポップアップを廃止し、ヘッダのトグルアイコン1クリックで切り替える
- アイコンはライトモード時に月（押すとダークへ）、ダークモード時に太陽（押すとライトへ）を表示する。両方の SVG を HTML に同居させ、`data-theme` 属性に応じて CSS で表示/非表示を切り替える方式
- データモデルは `data-theme` 属性ベースを維持しているため、将来テーマを再度増やすことは容易

## 印刷時レイアウト

`@media print` で印刷専用スタイルを定義する。レポート形式での書き出しに最適化されている。

- 非表示要素: ヘッダのナビゲーション、フッター、検索窓、操作ボタン群（編集/保存/削除/キャンセル）、キーボードショートカットトグル
- 余白: `@page { margin: 1.2cm 1.5cm }`、`main` の `max-width` を解除
- 配色: 背景色をすべて透明化、リンクは黒文字+下線、WikiLink の点線下線も実線に
- コードブロックと Callout は背景透明・枠線のみ
- フォントサイズは 11pt 相当、行間 1.7 に縮小
- 改ページ制御: `h1`, `h2`, `h3` は直後の要素と離れないよう `page-break-after: avoid`。`h2`, `h3`, `pre`, `table`, `.callout`, `.related-pages` には `page-break-inside: avoid`

## アクセス保護

個人用途を想定し、HTTPアクセス制限を簡易に実現するための機構を起動時オプションで提供する。サーバ本体はミドルウェアを差し込むのみで、設定が空のときは挙動が変わらない。

### IP制限

- `-allow-ip` フラグで CIDR をカンマ区切りで指定する（例: `127.0.0.1/32,192.168.1.0/24`）
- 空のとき（デフォルト）はIP制限を行わない
- 単一IPでも CIDR 表記が必要（例: `10.0.0.5/32`）
- `RemoteAddr` をパースして `net.IPNet.Contains` で判定。一致しなければ 403
- 個人用途想定のため `X-Forwarded-For` ヘッダは見ない（プロキシ越し構成は対象外）
- 起動時に CIDR 表記をパースし、不正なら起動を停止する（意図せぬ全許可を防ぐ）
- 注意: macOSの `localhost` は通常 `::1`（IPv6）に解決されるため、IPv4のみ許可するときは `127.0.0.1` で明示アクセスするか、IPv6も併記する（例: `127.0.0.1/32,::1/128`）

### Basic認証

- `-basic-auth "user:pass"` フラグで指定する。空のとき（デフォルト）はBasic認証を行わない
- `go-chi/middleware.BasicAuth` を realm `"ivvvy"` で利用
- `:` を含まない指定や、ユーザ名/パスワードが空の指定は起動時にエラー
- 認証失敗時は標準の 401 + `WWW-Authenticate` ヘッダ

### 併用

IP制限とBasic認証は併用可能。両方を有効にすると **AND条件**（IPが許可レンジ内 かつ 正しい認証情報）でのみアクセスを許可する。

## 静的サイト生成

`-generate` フラグで出力ディレクトリを指定すると、サーバーを起動せずに全ページを静的HTMLサイトとして出力する。

### 出力ディレクトリ構造

```
output/
├── index.html                # トップページ（検索画面）
├── pages/index.html          # 全ページ一覧
├── tags/
│   ├── index.html            # タグ一覧
│   └── {tag}/index.html      # タグ別ページ一覧
├── graph/index.html          # グラフビュー画面
├── page/{id}/index.html      # 各ページ
├── static/css/style.css      # CSS
├── static/js/*.js            # JavaScript（cytoscape.min.js, graph.js を含む）
├── api/graph.json            # グラフデータ
└── search/index.json         # 検索インデックス
```

### 静的サイトの特徴

- 「新規作成」ナビリンクと「編集」ボタン、「履歴」リンクが非表示になる（`IsStatic` フラグによるテンプレート条件分岐）
- ディレクトリベースURL（`page/{id}/index.html`）により、静的ファイルサーバーでクリーンURLが維持される
- クライアントサイド検索は `search/index.json` を読み込むためそのまま動作する（ただし `file://` プロトコルでは CORS 制限により動作しない）
- タグ一覧・タグ別ページ一覧も静的HTMLとして生成される
- ページ単位の版管理（履歴）は静的サイトには含まれない
- 実装は `internal/generate/generate.go` に集約されている
- サーバーと同じテンプレート・CSS・JSを使用するため、見た目はサーバー版と同一

## 設計判断

### データベースを使わない理由

- LLMとの親和性を重視（テキストファイルは直接読み書き可能）
- バックアップやマイグレーションが単純なファイルコピーで済む
- 外部データベースへの依存を排除し、シングルバイナリの利便性を維持

### オンメモリキャッシュの理由

- 個人用途のドキュメント管理ツールではページ数が限定的（数百〜数千程度）
- 全ページをメモリに保持しても十分にメモリ消費量が小さい
- 読み取りのたびにディスクI/Oが発生するのを排除し、レスポンスを高速化
- 単一プロセス・単一ユーザーの前提により、キャッシュの一貫性制御がシンプル

### kagome + クライアントサイド検索の理由

- 日本語はスペース区切りがないため、形態素解析なしでは検索精度が低い
- kagomeでサーバー側トークン化 → JSONでクライアントに配信の方式により、
  検索時のサーバー負荷をゼロにしつつ日本語検索精度を確保

### サーバーサイドプレビューの理由

- クライアントサイドの簡易正規表現変換ではGFMテーブル・h4以降の見出し等が未対応だった
- サーバー側のgoldmarkでHTML変換することで、閲覧時と同一の結果をプレビューに表示可能
- タブ切り替え方式により、プレビュー表示時のみAPIコールが発生するため負荷は低い

### ID自動採番の理由

- ユーザーが一意なIDを考える手間を省く
- ミリ秒タイムスタンプは高頻度でなければ十分に一意
- 詳細ページにIDを表示し、WikiLink作成時にコピーして使う運用
